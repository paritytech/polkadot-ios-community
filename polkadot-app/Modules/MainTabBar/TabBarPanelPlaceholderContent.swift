import SwiftUI
import DesignSystem

/// Placeholder for the real panel content. Replace with actual implementation.
struct TabBarPanelPlaceholderView: View, Hashable {
    var body: some View {
        Text("Panel Content")
            .textStyle(.body14Regular())
            .foregroundStyle(.fgPrimary)
            .frame(maxWidth: .infinity)
            .frame(height: 200)
    }
}
