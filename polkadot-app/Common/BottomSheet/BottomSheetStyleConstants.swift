import UIKit

enum BottomSheetStyleConstants {
    static let cornerRadius: CGFloat = 40
    static let backgroundInsets = UIEdgeInsets(top: 0, left: 8, bottom: 8, right: 8)
    static let contentInsets = UIEdgeInsets(top: 40, left: 24, bottom: 24, right: 24)
}
