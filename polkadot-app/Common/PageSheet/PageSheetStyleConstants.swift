import UIKit

enum PageSheetStyleConstants {
    static let cornerRadius: CGFloat = 32
    static let backgroundInsets = UIEdgeInsets(top: 8, left: 8, bottom: 8, right: 8)
    static let contentInsets = UIEdgeInsets(top: 8, left: 24, bottom: 0, right: 24)
    static let indicatorSize = CGSize(width: 48, height: 4)
    static let indicatorInsets = UIEdgeInsets(top: 16, left: 0, bottom: 8, right: 0)
}
