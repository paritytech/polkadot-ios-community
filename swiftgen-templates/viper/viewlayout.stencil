import SwiftUI

public struct {{moduleName}}ViewLayout: View {
    @State var viewModel = {{moduleName}}ViewModel()

    public init() {
    }

    public var body: some View {
        ScrollView(.vertical) {
            VStack(alignment: .center) {
                // Add your view content here
            }
        }
        .contentMargins(.horizontal, 16)
        .contentMargins(.top, 8)
    }
}

#Preview {
    {{moduleName}}ViewLayout()
}

